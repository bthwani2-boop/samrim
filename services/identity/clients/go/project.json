{
  "name": "identity-go-client",
  "root": "services/identity/clients/go",
  "projectType": "library",
  "implicitDependencies": [
    "identity"
  ],
  "targets": {
    "vet": {
      "executor": "nx:run-commands",
      "inputs": [
        "default",
        "^default",
        "goToolchain"
      ],
      "options": {
        "command": "go vet ./...",
        "cwd": "services/identity/clients/go"
      }
    },
    "unit": {
      "executor": "nx:run-commands",
      "inputs": [
        "default",
        "^default",
        "goToolchain"
      ],
      "options": {
        "command": "go test ./...",
        "cwd": "services/identity/clients/go"
      }
    },
    "format-check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node tools/dev/check-go-format.mjs services/identity/clients/go"
      }
    }
  },
  "tags": [
    "scope:identity-go-client",
    "type:service"
  ],
  "namedInputs": {
    "default": [
      "{projectRoot}/**/*",
      "{workspaceRoot}/go.work",
      "{workspaceRoot}/go.work.sum"
    ]
  }
}
