# DSH Contracts

This room is the canonical DSH service contract authority.

Business operations, schemas, authorization metadata, and generated lineage must converge on service-owned sources rather than manual parallel registries.

Do not preserve app-shaped or actor-shaped contract authority merely because inherited files used those names.
