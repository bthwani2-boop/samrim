# Clean-Target Repository Topology Specialization

TEMPORARY_TARGET_SPECIALIZATION: YES
GENERAL_TOPOLOGY_AUTHORITY: governance/architecture/REPOSITORY-TOPOLOGY.md
GENERAL_EXECUTION_AUTHORITY: tools/prompting/bthwani-orchestrator/03-LIVE-EXECUTION-RESTRUCTURE-CLEANUP.md
IMPLEMENTATION_STATE_AUTHORITY: NONE

## Purpose

Apply the durable repository-topology authority to the current clean-target reconstruction. This file owns only donor/target reconstruction consequences and temporary qualification guidance. It must not become the only durable owner of the final repository shape.

## Clean-target construction law

Build directly in the canonical durable owner location. Do not recreate a known losing donor path merely to move it again.

```text
DONOR_PATH != TARGET_PATH_AUTHORITY
DONOR_REQUIRED_VALUE → EXTRACT
TARGET_CANONICAL_OWNER → BUILD/REIMPLEMENT/REGENERATE
DONOR_LOSER → DO_NOT_IMPORT
TARGET_LOSER → CUT_OVER/DELETE
```

## Donor losing-container translation

When required donor value is found in historical/generic shapes, use these reconstruction directions only after semantic ownership proof:

```text
core/identity          → services/identity
core/workforce         → services/workforce
core/platform-control  → Platform Control semantic owner; services/platform-control only if service admission passes
core/providers         → operation-owning service integration ports/adapters
shared/ui-kit          → packages/design-system when reusable visual-system admission passes
shared/control-panel   → app host, Design System or service-owned presentation according to responsibility
shared/data-runtime    → precise technical/data owner; never one renamed generic package
shared/resilience      → proven reusable technical package, owner-local mechanism or mature dependency; no generic policy owner
apps/<app>/runtime     → direct apps/<app>/ host when runtime is only a pass-through layer
```

No mapping grants automatic survival to the destination either; each target container must satisfy the durable admission law.

## Platform Control service-admission split

Two questions are independent:

```text
PLATFORM_CONTROL_SEMANTIC_RESPONSIBILITY_ADMITTED?
PLATFORM_CONTROL_INDEPENDENT_DEPLOYABLE_SERVICE_ADMITTED?
```

The semantic control-plane responsibility may remain durable while physical deployment is rehomed if executable evidence does not justify an independent service. Never infer service admission from old `core/platform-control`, current Governance wording or a temporary target folder.

## Target foundation shape

Foundation construction may materialize the canonical top-level taxonomy and non-semantic service/app/technical shells without furnishing Product capabilities. Empty/placeholder subtrees are not created merely to make a diagram look complete.

Required foundation proof includes:

```text
DURABLE_REPOSITORY_TOPOLOGY_OWNER=PASS
TOP_LEVEL_TAXONOMY=PASS
INVALID_GENERIC_core/shared_OWNERSHIP_CLASSES=0
DIRECT_DEPLOYABLE_APP_ROOTS=PASS
PASS_THROUGH_apps/*/runtime_LAYER_WITHOUT_UNIQUE_ROLE=0
PACKAGES_ONLY_PROVEN_TECHNICAL_REUSE=PASS
ROOT_contracts_NO_BUSINESS_API_AUTHORITY=PASS
INFRA_NO_BUSINESS_SEMANTICS=PASS
SERVICE→APP_DEPENDENCIES=0
```

## Reconstruction completion

Before this temporary specialization can be deleted:

1. every surviving target root/container has a durable owner from Governance/executable source;
2. no final placement law exists only in this file;
3. all donor value needed for topology decisions is dispositioned;
4. all consumers/runtime/workspace/build references use canonical locations;
5. losing target containers/aliases/wrappers are absent;
6. the durable topology owner plus current executable repository are sufficient to reconstruct the intended shape without this package.
