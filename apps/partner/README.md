# Partner App

Partner/vendor-facing deployable application host.

Owns partner-specific composition only: application bootstrap, navigation, routes, deep links, native bindings, app-specific assets, and deployable configuration.

Partner capabilities consume canonical service/package/contract boundaries. Actor-specific presentation does not create a second domain owner.
