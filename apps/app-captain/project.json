{
  "name": "app-captain",
  "root": "apps/app-captain",
  "sourceRoot": "apps/app-captain",
  "projectType": "application",
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir apps/app-captain typecheck"
      }
    },
    "export-smoke": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node tools/mobile/export-mobile-smoke.mjs app-captain"
      }
    },
    "unit": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node tools/mobile/test-mobile-app.mjs app-captain"
      },
      "inputs": [
        "default",
        "^default",
        "{workspaceRoot}/tools/mobile/test-mobile-app.mjs",
        "{workspaceRoot}/tools/mobile/define-samrim-expo-app.cjs",
        {
          "runtime": "node --version"
        },
        {
          "runtime": "pnpm --version"
        }
      ]
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec biome lint apps/app-captain --diagnostic-level=error"
      }
    }
  },
  "tags": [
    "scope:app-captain",
    "type:app"
  ]
}
