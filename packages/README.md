# Packages

Reusable libraries with a clear, unique responsibility live here.

A package exists only when its lifecycle and reuse boundary are justified. Do not use this root as a generic bucket for `shared`, `core`, `utils`, `helpers`, or duplicated domain logic.

Domain-owned mutable policy stays with its canonical domain/service owner.
