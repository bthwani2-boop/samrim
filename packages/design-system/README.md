# Design System

Canonical reusable visual-system authority for BThwani deployable surfaces.

Owns reusable semantic design tokens, themes, typography, icons/assets when truly shared, accessible primitives/components/patterns, RTL/LTR direction semantics, and justified web/native platform adaptations.

It does not own business state, permissions, domain validation, financial semantics, or repository-wide Product copy.
