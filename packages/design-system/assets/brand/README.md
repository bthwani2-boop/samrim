# Mobile brand asset sources

Each deployable mobile app owns separate editable `icon.png`, `adaptive-icon.png`, and `splash-icon.png` sources under its app-specific directory. The matching Expo files under `apps/*/assets` are generated projections; update the canonical source and run `pnpm run brand:generate`.
